***READ ME***
To run the program:

The file is a .ipynb. Run each cell in order. When you run it in Colab, 
it will request a dataset from the user. Select choose file and select Sample_Data_Set

This is a shortened version of the original data set that is quicker to run than the full dataset.